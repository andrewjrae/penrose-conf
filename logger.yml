refresh_rate: 30 seconds

appenders:
  logfile:
    kind: file
    path: "/home/ajrae/penrose-conf/logs/logs.log"
    encoder:
      pattern: "{d} - {m}{n}"

root:
  level: warn
  appenders:
    - logfile
